name: nom_nom
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  camera: ^0.11.2
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.0
  flutter_tts: ^4.2.3
 
  google_mlkit_image_labeling: ^0.14.1
  path: ^1.9.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/icons/